/**
 * Single source of the engine's version string, so a bundle, a page's <meta generator> and the
 * runtime API can never disagree about which engine produced an artifact.
 *
 * Pure module.
 */
export const ENGINE_VERSION_STRING = "twin-engine/v0.1";
