# OpenAI-era decision audit · 2026-08-18 (Claude)

Independent sweep of all docs on all 10 branches, the 6 Codex worktrees, and the archived `product-twin-lab` folders. Purpose: nothing decided or discovered in the ChatGPT/Codex era gets lost as Claude takes over execution (ChatGPT quota out until ~Aug 24). Owner has rejected per-manufacturer cold-email acquisition; this audit re-ranks the routes.

## 1. Acquisition re-ranking (replaces letter-first plan)

Old plan = one hero product gated on one email to Muuto. Single point of failure. New order, permission-light first:

1. **Photo-to-avatar today.** Pipeline + preflight + 17-check QA fully built; needs only photos of an owned product, a tape measure, `MESHY_API_KEY`. Zero external dependency; closes an exit gate. Subject = a **residential furniture piece the owner physically owns**, ideally one already in the twin index (POÄNG/KIVIK/LISABO/BILLY…) so the reconstruction cross-validates against the verified G2 envelope. ~~Anatomic Sitt~~ **RETRACTED 2026-08-18 (owner correction):** Anatomic Sitt makes special-needs seating/wheelchairs — wrong domain for a residential product graph. "Family firm = easy rights" was an audit error; rights convenience never overrides product relevance. Medtech stays in the Opero/Munin vertical.
2. **Roca + GF Sanipex professional-account downloads.** Blocked on a *login*, not a permission letter. Cheapest path to both G4 gates. Intake + QA contracts already built.
3. **Broaden the Shopify Model3D census** from 1 merchant (Design Public Group: 8 native-3D finds, 6 scale-QA'd, NORR11 dimension targets already published) to the top ~20 design merchants. Pure compute. Resolve rights with the **merchant** (commercial incentive) rather than the manufacturer (none).
4. **Platform applications, not emails:** BIMobject Developer API (`ACCESS_REQUEST_REQUIRED`, never applied — best G4 route for windows/sanitary/HVAC) and pCon (solves the many-finishes contract-furniture problem once). Also CADENAS 3Dfindit partnership = hundreds of manufacturer emails replaced by one.
5. **Write the one-page manufacturer VALUE offer** before any Muuto/Arper contact: verified twins + placement in real for-sale properties + swap/behaviour analytics = something manufacturers *get*, not a favour they grant. The strategy docs describe this offer; the drafted letters don't make it. Muuto becomes the reward, not the dependency.
6. **Finish the 8 missing Kator/Legaz review claims** (hours of paperwork) → immediate rights-safe visual breadth in the Design Asset lane.
7. **Leroy Merlin/ADEO Mirakl syndication route** (documented, untouched): offer suppliers AI-ready enrichment, receive dated stock/order evidence back — the scalable value-exchange version of "supply access".

## 2. Landmines found (fix or acknowledge)

- **Furniture Avatar Package #2 is BLOCKED by independent review**, incl. schema invalid against itself and canonical "front" views rendered from the REAR (KIVIK back labelled front). Avatar Factory docs still say PASS. Verification wins per AGENTS.md. Any agent-authored visual QA must be independently re-derived or human-viewed.
- **The live Room Lab is showing supply evidence that expired 2026-08-17T23:59:59** — stale by the system's own rules since yesterday.
- **Root-folder docs in `/AI/product twin/` are superseded snapshots** (older coverage/status numbers than repo `docs/`). Archive only.
- Avatar counts disagree across docs (22 vs 23 promoted); branch names drift in 4 handoffs; Visual Media's named base `c90c72c` doesn't exist; Room Alpha V14's commit is unfetchable from GitHub.
- `docs/PERSISTENT-CHAT-TOPOLOGY.md` + `docs/BRAIN-CONTROL-BOARD.md` exist **only on `agent/product-twin-integration`** — a fresh `main` clone can't find the coordination surface. Merge them to `main`.
- Kator/Legaz: claimed G1, independently verified G0, 0/12 publishable.
- 12 of 22 promoted avatars are IKEA — "source-neutral graph" currently = one retailer.
- Evidence machinery >> evidence: thousands of assertions guarding 0 G3, 0 procurement-ready lines. Shift ratio from gate-building to gate-passing.

## 3. Buried gold (surface these)

- **`agent/turis-essence-pilot` proposal** (only on `agent/claude-concept-casas` learnings doc): furnish one room of a real builder project the owner already represents → per-villa film → neighbourhood assembly. Closes repo gates AND opens builder-as-customer. Possibly the best commercial idea in the corpus; referenced nowhere.
- **The cart half of the loop is already proven**: `exact_cart_proof_exists: true`; only authoritative shipping is missing. The prose overstates the gap.
- **`product-twin-lab-v0.9.x` archives hold a prior generation**: an iOS capture app, RENOVATION-ENGINE.md, SPATIAL-TWIN.md, EXISTING-OBJECT-MATCHING.md, and 26 numbered decisions (D001–D026) never carried into the repo — incl. demand-wedge-is-Spanish-property (D002), 20–50-twins-first MVP (D006), physical-fit-outranks-visual-similarity (D024 area). Port these docs into `repo/docs/archive/` before they're lost.
- Second candidates nobody counts: **Arper Ralik #7417** and **GF Sanipex 4700-096** both have targets + capture metrics.
- Apple Object Capture on owner's own devices = a second permission-free reconstruction route no status doc mentions.
- "Publish the gaps — the what-is-known/what-is-open page is the most persuasive page in any client document" (learnings doc). Use it.
- The two root PDFs (`ai house brainstorm 1.pdf`, `3d print house brainstorm.pdf`) are the unread primary sources behind House.exe/HouseKit.
- Media-pipeline lessons from the resort study (scene-life-first prompts, camera move last, ffmpeg traps) are not referenced by Visual Media Studio's handoff.

## 4. Deadlines & rot

- Lantmäteriet old product models withdrawn **2026-11-30**; SMHI Mesan2gv2 **2026-11-01**.
- Price/stock observations: stale after 1 day by protocol — most are from 2026-08-17.
- ChatGPT worktrees effectively frozen until ~Aug 24; Claude branch = `agent/spatial-studio-claude`, clone at `/AI/product twin/repo/`.
